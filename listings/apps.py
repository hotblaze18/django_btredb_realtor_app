from django.apps import AppConfig

# Have not changed this file at all, just added the Listing config
# to apps in settings.(Djangos app concept)


class ListingsConfig(AppConfig):
    name = 'listings'
